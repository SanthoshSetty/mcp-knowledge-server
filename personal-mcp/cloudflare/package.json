{
  "name": "personal-mcp-worker",
  "version": "1.0.0",
  "description": "Personal AI Assistant MCP Server - Cloudflare Workers Edition",
  "main": "src/worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.0.0"
  }
}
